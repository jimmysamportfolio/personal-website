"use client";

import { Section } from "@/components/DynamicSectionHeader";

// Add project-specific dynamic sections here.
// Each section can be used inside MDX files via <DynamicSectionHeader />.
export const maSections: Section[] = [];
